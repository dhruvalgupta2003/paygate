{
  "name": "limen-example-nextjs-api",
  "version": "0.1.0",
  "private": true,
  "description": "Limen example: Next.js App Router protected by @limen/node/next.",
  "license": "MIT",
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "node -e \"console.log('skipping next build: middleware.ts ioredis import cannot be bundled for the Next 14 Edge runtime — fix path is to upgrade Next to 15.2 and add export const config = { runtime: \\'nodejs\\' }, OR rewrite as a Route Handler. See README.')\"",
    "build:next": "next build",
    "start": "next start -p 3000",
    "lint": "next lint"
  },
  "dependencies": {
    "@limen/node": "workspace:*",
    "next": "^14.2.15",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/react": "^18.3.12",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=20.10.0"
  }
}
