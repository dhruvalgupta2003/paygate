[project]
name = "limen-example-flask"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "flask>=3.0",
  "limen[flask]",
  "gunicorn>=22",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
