"""Framework adapters.

Import the specific adapter you need, each lives in its own submodule so
optional framework dependencies don't pull into the core import path.
"""

from __future__ import annotations

__all__: list[str] = []
