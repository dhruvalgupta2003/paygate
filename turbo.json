{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env", "tsconfig.base.json"],
  "globalEnv": [
    "NODE_ENV",
    "LOG_LEVEL",
    "LIMEN_WALLET_ADDRESS",
    "LIMEN_CHAIN",
    "LIMEN_RPC_URL",
    "LIMEN_FACILITATOR_URL",
    "LIMEN_DATABASE_URL",
    "LIMEN_REDIS_URL",
    "LIMEN_ADMIN_SECRET"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "lib/**", ".next/**", "!.next/cache/**"],
      "inputs": [
        "src/**",
        "tsconfig*.json",
        "package.json",
        "tsup.config.*",
        "vite.config.*"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": ["coverage/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "clean": {
      "cache": false
    }
  }
}
